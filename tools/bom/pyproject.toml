[project]
name = "smart-greenhouse-bom-tools"
version = "0.1.0"
description = "BOM generation tools for the smart-greenhouse repository."
requires-python = ">=3.12,<3.13"
dependencies = [
    "openpyxl",
]

[tool.uv]
package = false
