[project]
name = "smart-greenhouse-scripts"
version = "0.1.0"
description = "Utility scripts for the smart-greenhouse repository."
requires-python = ">=3.12,<3.13"
dependencies = [
    "openpyxl",
]

[tool.uv]
package = false
