# Helpers for greenhouse watering — include in configuration.yaml:
# input_number: !include input_number.yaml
#
# Entity ID after load: input_number.greenhouse_irrigation_volume

greenhouse_irrigation_volume:
  name: "Теплица — объём полива"
  min: 0.5
  max: 100
  step: 0.5
  unit_of_measurement: "L"
  icon: mdi:water-pump
  initial: 5
  mode: box
  name: "Теплица — объём полива"
  min: 0.5
  max: 100
  step: 0.5
  unit_of_measurement: "L"
  icon: mdi:water-pump
  initial: 5
  mode: box
