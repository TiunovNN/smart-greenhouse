# Generated by aurora@aurora-smart-home (home-assistant skill)
# https://github.com/tonylofgren/aurora-smart-home
#
# Greenhouse setpoints — include in configuration.yaml:
#   input_number: !include input_number.yaml
#
# Defaults are applied from input_select.greenhouse_plant_profile via
# automation greenhouse_apply_plant_profile (see plant_profiles.yaml).
# Manual tweaks persist until the profile is switched again.

greenhouse_irrigation_volume:
  name: "Теплица — объём полива"
  min: 0.5
  max: 100
  step: 0.5
  unit_of_measurement: "L"
  icon: mdi:water-pump
  initial: 6
  mode: box

greenhouse_irrigation_humidity_min:
  name: "Теплица — полив при RH ниже"
  min: 40
  max: 90
  step: 1
  unit_of_measurement: "%"
  icon: mdi:water-percent
  initial: 68
  mode: box
  # GH interior RH typically 70–95%; threshold = relative drop proxy for soil drying

greenhouse_vent_temp_open:
  name: "Теплица — проветривание при max T выше"
  min: 18
  max: 35
  step: 0.5
  unit_of_measurement: "°C"
  icon: mdi:thermometer-high
  initial: 26
  mode: box

greenhouse_vent_temp_full:
  name: "Теплица — полное открытие при max T выше"
  min: 22
  max: 40
  step: 0.5
  unit_of_measurement: "°C"
  icon: mdi:thermometer-alert
  initial: 30
  mode: box

greenhouse_vent_humidity_max:
  name: "Теплица — проветривание при средней RH выше"
  min: 60
  max: 95
  step: 1
  unit_of_measurement: "%"
  icon: mdi:air-humidifier
  initial: 82
  mode: box
  # Upper-band trigger in 70–95% GH range; not "low humidity" semantics

# Placeholder until a soil moisture sensor is wired (see docs/01-overview.md §4.6).
# Not used by automations today — irrigation uses average air RH from SHT31 as proxy.
greenhouse_soil_moisture_min:
  name: "Теплица — целевая влажность почвы (резерв)"
  min: 20
  max: 90
  step: 1
  unit_of_measurement: "%"
  icon: mdi:flower
  initial: 60
  mode: box
